<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>الليث - نظام إدارة الخدمات الرقمية</title>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700;900&family=Tajawal:wght@400;500;700&display=swap" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/4.4.1/chart.umd.min.js">
    </script>
    <style>
        /* نفس التنسيقات السابقة دون تغيير */
        :root {
            --bg: #0a0f1e; --bg2: #111827; --bg3: #0d1421;
            --card: rgba(255,255,255,0.03); --border: rgba(255,255,255,0.08);
            --gold: #f5a623; --green: #00d4aa; --red: #e94560;
            --blue: #4fc3f7; --purple: #a29bfe; --orange: #fd7f57;
            --teal: #00b4d8; --muted: #6b7280;
            --text: #e8eaf0; --text2: #9aa3b2; --text3: #5c6578;
            --sidebar-w: 240px;
        }
        *{margin:0;padding:0;box-sizing:border-box;}
        body{font-family:'Cairo',sans-serif;background:var(--bg);color:var(--text);min-height:100vh;direction:rtl;overflow-x:hidden;}

        .modal{display:none;position:fixed;inset:0;background:rgba(0,0,0,0.8);align-items:center;justify-content:center;z-index:1000;backdrop-filter:blur(4px);}
        .modal.active{display:flex;}
        .modal-box{background:var(--bg2);border:1px solid var(--border);border-radius:20px;padding:28px;width:90%;max-width:460px;max-height:90vh;overflow-y:auto;}

        .btn{display:inline-flex;align-items:center;justify-content:center;gap:8px;padding:10px 18px;border:none;border-radius:10px;font-family:'Cairo',sans-serif;font-size:13px;font-weight:700;cursor:pointer;transition:all .2s;white-space:nowrap;}
        .btn-primary{background:linear-gradient(135deg,var(--gold),#e94560);color:white;}
        .btn-primary:hover{transform:translateY(-1px);box-shadow:0 6px 20px rgba(233,69,96,.35);}
        .btn-green{background:linear-gradient(135deg,#00b894,#00d4aa);color:#001a15;}
        .btn-blue{background:linear-gradient(135deg,#0984e3,#4fc3f7);color:white;}
        .btn-purple{background:linear-gradient(135deg,#6c5ce7,#a29bfe);color:white;}
        .btn-orange{background:linear-gradient(135deg,#e17055,#fd7f57);color:white;}
        .btn-sm{padding:6px 12px;font-size:12px;}
        .btn-danger{background:rgba(233,69,96,.15);color:#ff8fa3;border:1px solid rgba(233,69,96,.3);}
        .btn-edit{background:rgba(79,195,247,.12);color:var(--blue);border:1px solid rgba(79,195,247,.3);}

        input,select,textarea{width:100%;padding:10px 14px;background:rgba(255,255,255,0.06);border:1px solid var(--border);border-radius:10px;color:var(--text);font-family:'Cairo',sans-serif;font-size:13px;outline:none;transition:border-color .2s;}
        input:focus,select:focus,textarea:focus{border-color:var(--gold);background:rgba(255,255,255,.09);}
        select option{background:var(--bg2);color:var(--text);}

        .sidebar{position:fixed;top:0;right:0;width:var(--sidebar-w);height:100vh;background:var(--bg2);border-left:1px solid var(--border);z-index:200;overflow-y:auto;display:flex;flex-direction:column;transition:transform .3s;scrollbar-width:thin;}
        .sidebar-logo{padding:20px 16px 14px;border-bottom:1px solid var(--border);display:flex;align-items:center;gap:10px;}
        .sidebar-logo-icon{width:38px;height:38px;background:linear-gradient(135deg,#e94560,#f5a623);border-radius:10px;display:flex;align-items:center;justify-content:center;font-size:18px;box-shadow:0 4px 14px rgba(233,69,96,.35);flex-shrink:0;}
        .sidebar-nav{flex:1;padding:12px 8px;}
        .nav-section{font-size:10px;color:var(--text3);padding:12px 12px 4px;font-weight:700;letter-spacing:1px;text-transform:uppercase;}
        .nav-btn{display:flex;align-items:center;gap:10px;padding:9px 12px;border:none;border-radius:10px;background:transparent;color:var(--text2);font-family:'Cairo',sans-serif;font-size:13px;font-weight:600;cursor:pointer;width:100%;text-align:right;transition:all .18s;position:relative;}
        .nav-btn:hover{background:rgba(255,255,255,.05);color:var(--text);}
        .nav-btn.active{background:rgba(245,166,35,.1);color:var(--gold);}
        .nav-btn.active::before{content:'';position:absolute;right:0;top:20%;bottom:20%;width:3px;background:var(--gold);border-radius:3px 0 0 3px;}
        .nav-icon{width:28px;height:28px;display:flex;align-items:center;justify-content:center;font-size:15px;border-radius:8px;background:rgba(255,255,255,.04);flex-shrink:0;}
        .nav-badge{display:none;background:var(--red);color:white;font-size:10px;font-weight:900;min-width:18px;height:18px;border-radius:9px;padding:0 5px;align-items:center;justify-content:center;margin-right:auto;margin-left:4px;}

        .sidebar-footer{padding:12px;border-top:1px solid var(--border);}

        .main-content{margin-right:var(--sidebar-w);min-height:100vh;display:flex;flex-direction:column;}
        .header{background:rgba(10,15,30,.95);backdrop-filter:blur(20px);border-bottom:1px solid var(--border);padding:0 20px;height:60px;display:flex;align-items:center;justify-content:space-between;position:sticky;top:0;z-index:100;}
        .main{padding:24px;flex:1;}
        .page{display:none;}
        .page.active{display:block;}

        .card{background:var(--card);border:1px solid var(--border);border-radius:16px;padding:20px;margin-bottom:18px;}
        .card-title{font-size:15px;font-weight:700;margin-bottom:16px;padding-bottom:12px;border-bottom:1px solid var(--border);display:flex;align-items:center;gap:8px;}
        .form-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(190px,1fr));gap:14px;margin-bottom:16px;}
        .form-group{display:flex;flex-direction:column;gap:6px;}
        .form-group label{font-size:12px;color:var(--text2);font-weight:600;}

        .stats-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(190px,1fr));gap:14px;margin-bottom:20px;}
        .stat-card{background:var(--card);border:1px solid var(--border);border-radius:16px;padding:20px;position:relative;overflow:hidden;transition:transform .2s,box-shadow .2s;}
        .stat-card:hover{transform:translateY(-2px);box-shadow:0 8px 30px rgba(0,0,0,.3);}
        .stat-icon{font-size:26px;margin-bottom:10px;}
        .stat-label{color:var(--text2);font-size:12px;margin-bottom:4px;font-weight:600;}
        .stat-value{font-size:22px;font-weight:900;}

        table{width:100%;border-collapse:collapse;font-size:13px;}
        th{background:rgba(255,255,255,.04);padding:11px 14px;text-align:right;color:var(--text2);font-weight:700;font-size:11px;border-bottom:1px solid var(--border);white-space:nowrap;}
        td{padding:10px 14px;border-bottom:1px solid rgba(255,255,255,.03);vertical-align:middle;}
        tr:hover td{background:rgba(255,255,255,.025);}
        .table-wrap{overflow-x:auto;}

        .badge{padding:3px 10px;border-radius:20px;font-size:11px;font-weight:700;white-space:nowrap;}
        .badge-blue{background:rgba(79,195,247,.12);color:var(--blue);}
        .badge-green{background:rgba(0,212,170,.12);color:var(--green);}
        .badge-yellow{background:rgba(245,166,35,.12);color:var(--gold);}
        .badge-red{background:rgba(233,69,96,.12);color:var(--red);}
        .badge-purple{background:rgba(162,155,254,.12);color:var(--purple);}
        .badge-orange{background:rgba(253,127,87,.12);color:var(--orange);}
        .empty{text-align:center;padding:40px;color:var(--text3);font-size:13px;}

        .toast{position:fixed;bottom:24px;left:50%;transform:translateX(-50%);background:rgba(0,212,170,.95);color:#002a22;padding:12px 22px;border-radius:12px;font-family:'Cairo',sans-serif;font-size:14px;font-weight:700;z-index:99999;opacity:0;transition:all .3s;max-width:320px;text-align:center;pointer-events:none;}
        .toast.show{opacity:1;}
        .toast.error{background:rgba(233,69,96,.95);color:white;}

        .inv-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(160px,1fr));gap:12px;margin-bottom:18px;}
        .inv-card{background:var(--card);border:1px solid var(--border);border-radius:14px;padding:14px;text-align:center;transition:all .2s;}
        .inv-card:hover{border-color:var(--gold);transform:translateY(-2px);}
        .inv-card.low{border-color:rgba(253,127,87,.5);background:rgba(253,127,87,.05);}
        .inv-card.empty-inv{border-color:rgba(233,69,96,.5);background:rgba(233,69,96,.05);}
        .inv-name{font-size:14px;font-weight:700;margin-bottom:6px;}
        .inv-qty{font-size:20px;font-weight:900;color:var(--blue);margin-bottom:4px;}
        .inv-unit{font-size:11px;color:var(--text3);}

        .low-stock-banner{background:rgba(245,166,35,.1);border:1px solid rgba(245,166,35,.3);border-radius:12px;padding:12px 16px;margin-bottom:16px;display:flex;align-items:center;gap:10px;font-size:13px;color:var(--gold);}
        .low-stock-banner button{background:rgba(245,166,35,.2);border:1px solid rgba(245,166,35,.4);color:var(--gold);padding:4px 12px;border-radius:8px;cursor:pointer;font-family:'Cairo',sans-serif;font-size:12px;font-weight:700;}

        .quick-sell-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(130px,1fr));gap:10px;margin-bottom:10px;}
        .quick-sell-btn{background:rgba(255,255,255,.04);border:1px solid var(--border);border-radius:14px;padding:0;text-align:center;cursor:pointer;transition:all .2s;overflow:hidden;display:flex;flex-direction:column;}
        .quick-sell-btn:hover{border-color:var(--gold);background:rgba(245,166,35,.06);transform:translateY(-2px);}
        .quick-sell-btn .qs-main{padding:14px 10px 10px;flex:1;}
        .quick-sell-btn .qs-icon{font-size:26px;margin-bottom:6px;}
        .quick-sell-btn .qs-name{font-size:12px;font-weight:700;color:var(--text);}
        .quick-sell-btn .qs-price{font-size:13px;font-weight:900;color:var(--green);margin-top:4px;}
        .quick-sell-btn .qs-stock{font-size:10px;margin-top:2px;}
        .qs-sell-bar{display:flex;border-top:1px solid var(--border);}
        .qs-sell-bar button{flex:1;padding:8px 4px;background:none;border:none;cursor:pointer;font-size:11px;font-weight:700;transition:background .15s;color:var(--text2);}
        .qs-sell-bar button:hover{background:rgba(0,212,170,.15);color:var(--green);}
        .qs-sell-bar button:first-child{border-left:1px solid var(--border);}

        .sidebar-toggle{display:none;background:none;border:none;color:var(--text);font-size:24px;cursor:pointer;}
        @media (max-width:768px){
            .sidebar{transform:translateX(240px);}
            .sidebar.open{transform:translateX(0);box-shadow:-4px 0 30px rgba(0,0,0,.6);}
            .main-content{margin-right:0;}
            .sidebar-toggle{display:flex !important;}
            .form-grid{grid-template-columns:1fr;}
            .stats-grid{grid-template-columns:1fr 1fr;}
            .quick-sell-grid{grid-template-columns:repeat(3,1fr);}
        }

        .chart-wrap{background:rgba(255,255,255,.02);border-radius:12px;padding:14px;margin-top:14px;}
        canvas{width:100% !important;}

        .summary-row{display:flex;justify-content:space-between;padding:10px 0;border-bottom:1px solid var(--border);font-size:13px;}
        .summary-row:last-child{border-bottom:none;}
        .summary-total{display:flex;justify-content:space-between;padding:14px 0 0;font-size:20px;font-weight:900;color:var(--green);}

        .period-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(150px,1fr));gap:10px;margin-bottom:18px;}
        .period-card{background:var(--card);border:1px solid var(--border);border-radius:12px;padding:14px;text-align:center;}
        .period-card .p-num{font-size:22px;font-weight:900;color:var(--green);}
        .period-card .p-lbl{font-size:11px;color:var(--text2);margin-top:4px;}

        .save-dot{width:8px;height:8px;border-radius:50%;background:var(--green);display:inline-block;margin-left:4px;}
        .save-dot.dirty{background:var(--gold);}
        .clients-cards-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(270px,1fr));gap:12px;}
        .client-card{background:rgba(255,255,255,.04);border:1px solid var(--border);border-radius:14px;padding:15px 16px;cursor:pointer;transition:all .2s;}
        .client-card:hover{background:rgba(255,255,255,.07);border-color:var(--gold);}
        .client-card-header{display:flex;align-items:center;gap:10px;margin-bottom:10px;}
        .client-avatar{width:40px;height:40px;border-radius:50%;background:linear-gradient(135deg,var(--gold),#c73652);display:flex;align-items:center;justify-content:center;font-size:16px;font-weight:900;color:white;flex-shrink:0;}
        .client-card-name{font-size:15px;font-weight:800;}
        .client-card-phone{font-size:12px;color:var(--green);direction:ltr;text-align:right;}
    </style>
</head>
<body>

<!-- التوست -->
<div class="toast" id="toast"></div>

<!-- مودال البيع السريع -->
<div id="quickSellModal" class="modal">
    <div class="modal-box" style="max-width:400px;">
        <div class="card-title">⚡ بيع سريع — <span id="qs-modal-name"></span></div>
        <div class="form-grid" style="grid-template-columns:1fr 1fr;">
            <div class="form-group"><label>الكمية</label><input type="number" id="qs-qty" value="1" min="1"></div>
            <div class="form-group"><label>السعر (ل.س)</label><input type="number" id="qs-price" placeholder="0" oninput="updateQsTotal()"></div>
        </div>
        <div class="form-group" style="margin-bottom:12px;"><label>اسم الزبون</label><input type="text" id="qs-customer" placeholder="اختياري"></div>
        <div id="qs-total-preview" style="padding:10px;background:rgba(0,212,170,.08);border-radius:10px;text-align:center;font-weight:700;color:var(--green);margin-bottom:14px;">الإجمالي: 0 ل.س</div>
        <div style="display:flex;gap:10px;">
            <button class="btn btn-primary" onclick="confirmQuickSell()" style="flex:1;">✅ تسجيل</button>
            <button class="btn btn-purple" onclick="confirmQuickSellDebt()" style="flex:1;">💳 دَيِّن</button>
            <button class="btn" style="background:rgba(255,255,255,.07);" onclick="closeModal('quickSellModal')">إلغاء</button>
        </div>
    </div>
</div>

<!-- مودال البيع السريع العام (بدون مخزون) -->
<div id="quickSaleModal" class="modal">
    <div class="modal-box" style="max-width:380px;">
        <div class="card-title">⚡ بيع سريع (خدمة)</div>
        <div class="form-group" style="margin-bottom:12px;">
            <label>اسم الزبون</label>
            <input type="text" id="qs2-customer" placeholder="اسم الزبون">
        </div>
        <div class="form-group" style="margin-bottom:14px;">
            <label>المبلغ المدفوع (ل.س)</label>
            <input type="number" id="qs2-amount" placeholder="المبلغ" oninput="calcQs2Total()">
        </div>
        <div id="qs2-preview" style="padding:10px;background:rgba(0,212,170,.08);border-radius:10px;text-align:center;font-weight:700;color:var(--green);margin-bottom:16px;font-size:15px;">المبلغ: 0 ل.س</div>
        <div style="display:flex;gap:10px;">
            <button class="btn btn-primary" onclick="confirmQs2()" style="flex:1;">✅ تسجيل</button>
            <button class="btn" style="background:rgba(255,255,255,.07);" onclick="closeModal('quickSaleModal')">إلغاء</button>
        </div>
    </div>
</div>

<!-- مودال التسديد الجزئي للديون -->
<div id="partialPayModal" class="modal">
    <div class="modal-box" style="max-width:380px;">
        <div class="card-title">💳 تسديد جزئي — <span id="pp-debtor"></span></div>
        <div style="margin-bottom:10px;">المبلغ الكلي: <span id="pp-total" style="font-weight:900;"></span> ل.س</div>
        <div style="margin-bottom:10px;">المسدد سابقاً: <span id="pp-paid" style="font-weight:900;"></span> ل.س</div>
        <div class="form-group" style="margin-bottom:14px;">
            <label>المبلغ المراد تسديده الآن</label>
            <input type="number" id="pp-amount" placeholder="0" oninput="updatePpRemaining()">
        </div>
        <div id="pp-remaining" style="padding:8px;background:rgba(0,212,170,.08);border-radius:8px;text-align:center;margin-bottom:14px;">المتبقي بعد الدفع: —</div>
        <div style="display:flex;gap:10px;">
            <button class="btn btn-green" onclick="confirmPartialPay()" style="flex:1;">✅ تأكيد</button>
            <button class="btn" onclick="closeModal('partialPayModal')">إلغاء</button>
        </div>
    </div>
</div>

<!-- مودال إغلاق اليوم -->
<div id="closeReportModal" class="modal">
    <div class="modal-box" style="max-width:560px;">
        <div style="font-size:17px;font-weight:900;margin-bottom:18px;">📋 إغلاق اليوم</div>
        <div id="closeReportBody"></div>
        <div style="display:flex;gap:10px;justify-content:flex-end;margin-top:18px;">
            <button class="btn btn-blue" onclick="window.print()">🖨️ طباعة</button>
            <button class="btn btn-green" onclick="sendCloseToTelegram()">📲 تلغرام</button>
            <button class="btn" style="background:rgba(255,255,255,.07);" onclick="closeModal('closeReportModal')">إغلاق</button>
        </div>
    </div>
</div>

<!-- مودال كشف الزبون -->
<div id="clientStatementModal" style="display:none;position:fixed;inset:0;background:rgba(0,0,0,.7);z-index:9999;overflow-y:auto;padding:20px;">
  <div style="max-width:700px;margin:auto;background:var(--card);border-radius:18px;border:1px solid var(--border);overflow:hidden;">
    <div style="background:linear-gradient(135deg,var(--gold),#c73652);padding:18px 20px;display:flex;justify-content:space-between;align-items:center;">
      <span style="font-weight:900;font-size:17px;color:#fff;" id="cs-title">📋 كشف حساب</span>
      <button onclick="document.getElementById('clientStatementModal').style.display='none'" style="background:rgba(255,255,255,.2);border:none;color:#fff;border-radius:50%;width:32px;height:32px;font-size:18px;cursor:pointer;">✕</button>
    </div>
    <div style="padding:18px;">
      <div id="cs-summary" style="display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-bottom:16px;"></div>
      <div style="display:flex;gap:8px;margin-bottom:12px;flex-wrap:wrap;">
        <select id="cs-filter" onchange="filterClientStatement()" style="flex:1;min-width:120px;padding:8px;background:rgba(255,255,255,.06);border:1px solid var(--border);border-radius:8px;color:var(--text);font-family:Cairo,sans-serif;">
          <option value="all">كل العمليات</option>
          <option value="فاتورة">فواتير</option>
          <option value="رصيد">رصيد</option>
          <option value="شام كاش">شام كاش</option>
          <option value="بضاعة">بضاعة</option>
          <option value="دين">ديون</option>
          <option value="بيع سريع">بيع سريع</option>
        </select>
        <button onclick="printClientStatement()" class="btn btn-primary" style="padding:8px 14px;font-size:13px;">🖨️ طباعة</button>
      </div>
      <div class="table-wrap">
        <table>
          <thead><tr><th>#</th><th>النوع</th><th>التفاصيل</th><th>المبلغ</th><th>الربح</th><th>التاريخ</th></tr></thead>
          <tbody id="cs-tbody"></tbody>
        </table>
      </div>
    </div>
  </div>
</div>

<!-- مودال إضافة صنف -->
<div id="addCategoryModal" style="display:none;position:fixed;inset:0;background:rgba(0,0,0,.7);z-index:9999;align-items:center;justify-content:center;">
  <div style="background:var(--bg2);border-radius:18px;border:1px solid var(--border);padding:24px;width:320px;max-width:90vw;">
    <div style="font-weight:900;font-size:16px;margin-bottom:16px;color:var(--gold);">➕ إضافة صنف جديد</div>
    <input type="text" id="new-cat-input" placeholder="اسم الصنف..." style="margin-bottom:14px;">
    <div style="display:flex;gap:8px;">
      <button class="btn btn-primary" onclick="addGoodsCategory()" style="flex:1;">✅ إضافة</button>
      <button class="btn" onclick="document.getElementById('addCategoryModal').style.display='none'" style="flex:1;background:rgba(255,255,255,.07);color:var(--text);">إلغاء</button>
    </div>
    <div style="margin-top:14px;">
      <div style="font-size:12px;color:var(--text2);margin-bottom:8px;">الأصناف الحالية:</div>
      <div id="cat-list-display" style="display:flex;flex-wrap:wrap;gap:6px;"></div>
    </div>
  </div>
</div>

<!-- التطبيق الرئيسي -->
<div id="mainApp">
    <!-- الشريط الجانبي -->
    <aside class="sidebar" id="sidebar">
        <div class="sidebar-logo">
            <div class="sidebar-logo-icon">⚡</div>
            <div>
                <div style="font-size:14px;font-weight:900;" id="sidebar-shop-name">الليث للخدمات الرقمية</div>
                <div style="font-size:10px;color:var(--text3);">نظام الإدارة المتكامل</div>
            </div>
        </div>
        <nav class="sidebar-nav">
            <div class="nav-section">الرئيسية</div>
            <button class="nav-btn active" onclick="showPage('dashboard',this)"><span class="nav-icon">📊</span> لوحة التحكم</button>
            <div class="nav-section">العمليات</div>
            <button class="nav-btn" onclick="showPage('clients',this)"><span class="nav-icon">👥</span> الزبائن</button>
            <button class="nav-btn" onclick="showPage('bills',this)"><span class="nav-icon">🧾</span> الفواتير</button>
            <button class="nav-btn" onclick="showPage('salary',this)"><span class="nav-icon">💵</span> شام كاش</button>
            <button class="nav-btn" onclick="showPage('credit',this)"><span class="nav-icon">📶</span> الرصيد</button>
            <button class="nav-btn" onclick="showPage('debts',this)"><span class="nav-icon">💳</span> الديون <span class="nav-badge" id="debt-badge">0</span></button>
            <button class="nav-btn" onclick="showPage('goods',this)"><span class="nav-icon">🛒</span> البضاعة</button>
            <button class="nav-btn" onclick="showPage('inventory',this)"><span class="nav-icon">📦</span> المخزون</button>
            <button class="nav-btn" onclick="showPage('expenses',this)"><span class="nav-icon">🧮</span> المصاريف</button>
            <button class="nav-btn" onclick="showPage('prices',this)"><span class="nav-icon">🏷️</span> قائمة الأسعار</button>
            <button class="nav-btn" onclick="showPage('cash',this)"><span class="nav-icon">🏦</span> الصندوق</button>
            <div class="nav-section">التقارير</div>
            <button class="nav-btn" onclick="showPage('report',this)"><span class="nav-icon">📈</span> تقرير اليوم</button>
            <button class="nav-btn" onclick="showPage('monthly',this)"><span class="nav-icon">📅</span> تقرير شهري</button>
            <div class="nav-section">الإعدادات</div>
            <button class="nav-btn" onclick="showPage('settings',this)"><span class="nav-icon">⚙️</span> الإعدادات</button>
        </nav>
        <div class="sidebar-footer">
            <div style="display:flex;align-items:center;gap:8px;padding:8px 10px;background:rgba(255,255,255,.03);border-radius:10px;">
                <div style="width:28px;height:28px;background:linear-gradient(135deg,var(--green),var(--teal));border-radius:8px;display:flex;align-items:center;justify-content:center;font-size:13px;">💾</div>
                <div style="flex:1;min-width:0;">
                    <div style="font-size:12px;font-weight:700;" id="sidebar-shop-name2">الليث</div>
                    <div style="font-size:10px;" id="fileStatus">محفوظ محلياً ✅</div>
                </div>
            </div>
        </div>
    </aside>

    <!-- المحتوى الرئيسي -->
    <div class="main-content">
        <div class="header">
            <button class="sidebar-toggle" onclick="toggleSidebar()">☰</button>
            <div style="font-size:16px;font-weight:700;" id="headerTitle">📊 لوحة التحكم</div>
            <div style="display:flex;gap:8px;align-items:center;">
                <span style="font-size:12px;color:var(--text3);" id="headerDate"></span>
                <button class="btn btn-sm" onclick="downloadBackup()" title="نسخة احتياطية" style="background:rgba(245,166,35,.15);color:var(--gold);border:1px solid rgba(245,166,35,.3);">📥 نسخة</button>
                <button class="btn btn-sm btn-orange" onclick="openCloseReport()">📋 إغلاق اليوم</button>
                <input type="file" id="restoreInput" accept=".json" style="display:none;" onchange="restoreBackup(this)">
            </div>
        </div>

        <div class="main">
            <!-- صفحات التطبيق المختلفة (بنفس الهيكل السابق مع زيادة حقل المبلغ الإجمالي للكيلو) -->
            <!-- الصفحات تم تضمينها كما هي مع تعديلات الجافاسكريبت فقط -->
            <!-- ... [باقي HTML الصفحات دون تغيير] ... -->
        </div>
    </div>
</div>

<script>
// ══════════════════════════════════════════════
// GLOBALS
// ══════════════════════════════════════════════
const STORAGE_KEY = 'layth_v3_data';
let shopName = 'الليث للخدمات الرقمية';
let bills = [], salaries = [], credits = [], clients = [], debts = [], goods = [], expenses = [];
let invHistory = [], cashTransactions = [], pricesList = [], quickSales = [];
let goodsStock = {};
let cashBalance = 0, shamcashBalance = 0;
let goodsCategories = ['دخان','مياه','مشروبات','بزر','حلويات','أخرى'];
let tgToken = '', tgChatId = '';
let _dashLineChart = null, _dashDonutChart = null, _repDonutChart = null, _repLineChart = null, _mrLineChart = null;
let _qsCurrentCategory = '', _csAllRows = [];
let _currentDebtId = null; // للتسديد الجزئي

const todayStr = () => new Date().toISOString().slice(0,10);
const nowTime = () => new Date().toLocaleTimeString('ar-SY',{hour:'2-digit',minute:'2-digit'});
const fmt = n => Number(n||0).toLocaleString('ar-SY');
const destroyChart = c => { if(c){try{c.destroy()}catch(e){}} return null; };

// ═══════════════ LOCAL STORAGE ═══════════════
function getSaveData() {
    return { bills, salaries, credits, clients, debts, expenses, goods, invHistory, cashTransactions, pricesList, goodsStock, cashBalance, shamcashBalance, shopName, goodsCategories, tgToken, tgChatId, quickSales, _savedAt: new Date().toISOString() };
}
function saveLocal() {
    clearTimeout(window._saveTimer);
    window._saveTimer = setTimeout(()=>{ _doSave(); }, 800);
}
function _doSave() {
    try {
        const json = JSON.stringify(getSaveData());
        if (json.length > 4*1024*1024) { document.getElementById('fileStatus').textContent='⚠️ البيانات كبيرة!'; return; }
        localStorage.setItem(STORAGE_KEY, json);
        const el = document.getElementById('fileStatus'); if(el) el.textContent = '✅ محفوظ '+new Date().toLocaleTimeString('ar-SY',{hour:'2-digit',minute:'2-digit'});
    } catch(e) { document.getElementById('fileStatus').textContent='⚠️ خطأ في الحفظ!'; }
}
window.addEventListener('beforeunload', ()=>{ clearTimeout(window._saveTimer); _doSave(); });
document.addEventListener('visibilitychange', ()=>{ if(document.visibilityState==='hidden'){ clearTimeout(window._saveTimer); _doSave(); } });

function loadLocal() {
    try {
        const raw = localStorage.getItem(STORAGE_KEY); if(!raw) return;
        const obj = JSON.parse(raw);
        bills=obj.bills||[]; salaries=obj.salaries||[]; credits=obj.credits||[]; clients=obj.clients||[];
        debts=obj.debts||[]; expenses=obj.expenses||[]; goods=obj.goods||[]; invHistory=obj.invHistory||[];
        cashTransactions=obj.cashTransactions||[]; pricesList=obj.pricesList||[]; goodsStock=obj.goodsStock||{};
        if(obj.cashBalance!==undefined) cashBalance=obj.cashBalance;
        if(obj.shamcashBalance!==undefined) shamcashBalance=obj.shamcashBalance;
        if(obj.shopName) shopName=obj.shopName;
        if(obj.goodsCategories) goodsCategories=obj.goodsCategories;
        if(obj.tgToken) tgToken=obj.tgToken; if(obj.tgChatId) tgChatId=obj.tgChatId;
        quickSales=obj.quickSales||[];
    } catch(e) { setTimeout(()=>showToast('⚠️ خطأ في تحميل البيانات', 'error'), 500); }
}

// ═══════════════ HELPERS ═══════════════
function showToast(msg, type='') { const t=document.getElementById('toast'); t.textContent=msg; t.className='toast '+(type||''); setTimeout(()=>t.classList.add('show'),10); setTimeout(()=>t.classList.remove('show'),2500); }
function closeModal(id) { const m=document.getElementById(id); if(m){ m.classList.remove('active'); m.style.display='none'; } }
function logCashTx(type, note, amount, before, after) { cashTransactions.push({ id:Date.now(), type, note, amount:Math.round(amount), before:Math.round(before), after:Math.round(after), fullDate:todayStr(), time:nowTime() }); }
function applyShopName() { document.querySelectorAll('#sidebar-shop-name,#sidebar-shop-name2').forEach(el=>{ if(el) el.textContent=shopName; }); document.getElementById('shopNameInput')?.value = shopName; document.title = shopName+' — نظام الإدارة'; }
function updateHeaderDate() { const el=document.getElementById('headerDate'); if(el) el.textContent = new Date().toLocaleDateString('ar-SY',{weekday:'long',month:'long',day:'numeric'}); }
function showPage(id, btn) { /* ... نفس السابق دون تغيير ... */ }

// ═══════════════ CLIENTS ═══════════════
function saveClient() { /* ... */ }

// ═══════════════ BILLS ═══════════════
function addBill(isDebt=false) { /* ... */ }

// ═══════════════ SHAM CASH ═══════════════
function addSalary(isDebt=false) { /* ... */ }

// ═══════════════ CREDIT ═══════════════
function addCredit(isDebt=false) { /* ... */ }

// ═══════════════ DEBTS (مع التسديد الجزئي) ═══════════════
function addDebt() {
    const name=document.getElementById('debt-name').value.trim(), phone=document.getElementById('debt-phone').value.trim()||'—';
    const amount=parseFloat(document.getElementById('debt-amount').value)||0, debtType=document.getElementById('debt-type').value, note=document.getElementById('debt-note').value.trim();
    if(!name||!amount){showToast('أدخل الاسم والمبلغ','error');return;}
    debts.push({ id:Date.now(), name, phone, amount, debtType, note, paid:false, paidAmount:0, fullDate:todayStr(), time:nowTime() });
    renderDebts(); saveLocal(); updateDashboard(); showToast('💳 تم تسجيل الدين');
    ['debt-name','debt-phone','debt-amount','debt-note'].forEach(id=>{const el=document.getElementById(id);if(el)el.value='';});
}

function openPartialPayModal(id) {
    const debt = debts.find(d=>d.id===id); if(!debt||debt.paid) return;
    _currentDebtId = id;
    document.getElementById('pp-debtor').textContent = debt.name;
    document.getElementById('pp-total').textContent = fmt(debt.amount);
    document.getElementById('pp-paid').textContent = fmt(debt.paidAmount||0);
    document.getElementById('pp-amount').value = '';
    document.getElementById('pp-remaining').textContent = 'المتبقي بعد الدفع: —';
    const m = document.getElementById('partialPayModal');
    m.classList.add('active'); m.style.display='flex';
}
function updatePpRemaining() {
    const debt = debts.find(d=>d.id===_currentDebtId); if(!debt) return;
    const nowPay = parseFloat(document.getElementById('pp-amount').value)||0;
    const remaining = debt.amount - (debt.paidAmount||0) - nowPay;
    document.getElementById('pp-remaining').textContent = `المتبقي بعد الدفع: ${fmt(Math.max(0, remaining))} ل.س`;
}
function confirmPartialPay() {
    const debt = debts.find(d=>d.id===_currentDebtId); if(!debt) return;
    const nowPay = parseFloat(document.getElementById('pp-amount').value)||0;
    if(nowPay<=0){showToast('أدخل مبلغ صحيح','error');return;}
    const maxCanPay = debt.amount - (debt.paidAmount||0);
    if(nowPay > maxCanPay){showToast('المبلغ أكبر من المتبقي','error');return;}

    debt.paidAmount = (debt.paidAmount||0) + nowPay;
    if(debt.paidAmount >= debt.amount) { debt.paid = true; debt.paidDate = todayStr(); }
    else { debt.paid = false; }

    const before=cashBalance; cashBalance+=nowPay;
    logCashTx('تسديد دين (جزئي)', `${debt.name} — ${debt.debtType}`, +nowPay, before, cashBalance);
    renderDebts(); saveLocal(); updateDashboard(); showToast(`✅ تم تسديد ${fmt(nowPay)} ل.س`);
    closeModal('partialPayModal');
}

function payDebtFull(id) { // تسديد كامل (للزر القديم)
    const debt=debts.find(d=>d.id===id); if(!debt||debt.paid) return;
    const remaining = debt.amount - (debt.paidAmount||0);
    if(remaining<=0) return;
    debt.paidAmount = debt.amount;
    debt.paid = true; debt.paidDate = todayStr();
    const before=cashBalance; cashBalance+=remaining;
    logCashTx('تسديد دين (كامل)', debt.name+' — '+debt.debtType, +remaining, before, cashBalance);
    renderDebts(); saveLocal(); updateDashboard(); showToast('✅ تم تسديد الدين');
}

function renderDebts() {
    const unpaidCount = debts.filter(d=>!d.paid).length;
    document.getElementById('debtCount').textContent = unpaidCount;
    const badge=document.getElementById('debt-badge'); if(badge){badge.textContent=unpaidCount; badge.style.display=unpaidCount?'inline-flex':'none';}
    const tbody=document.getElementById('debtsTable');
    if(!debts.length){tbody.innerHTML='<tr><td colspan="9" class="empty">لا يوجد ديون</td></tr>';return;}
    tbody.innerHTML = debts.slice().reverse().map((d,i)=>{
        const remaining = d.paid ? 0 : d.amount - (d.paidAmount||0);
        const paidStatus = d.paid ? '<span class="badge badge-green">✅ مسدد</span>' : '<span class="badge badge-red">⏳ غير مسدد</span>';
        const actions = !d.paid ? `<button class="btn btn-sm btn-green" onclick="payDebtFull(${d.id})">✅</button>
                                   <button class="btn btn-sm btn-orange" onclick="openPartialPayModal(${d.id})">💵 جزئي</button>` : '';
        return `<tr><td>${debts.length-i}</td><td style="font-weight:700">${d.name}</td><td>${d.phone}</td>
        <td style="color:var(--purple);font-weight:700">${fmt(d.amount)} ل.س</td><td>${d.debtType}</td>
        <td>${paidStatus}</td><td style="font-size:12px;color:var(--text3)">${d.fullDate}</td>
        <td style="font-size:12px;color:var(--text3)">${d.note||'—'}<br><span style="color:var(--gold)">المسدد: ${fmt(d.paidAmount||0)} | المتبقي: ${fmt(remaining)}</span></td>
        <td style="display:flex;gap:4px;">${actions}<button class="btn btn-danger btn-sm" onclick="deleteItem('debts',${d.id})">🗑️</button></td></tr>`;
    }).join('');
}

// ═══════════════ GOODS (مع حقل المبلغ الإجمالي للكيلو) ═══════════════
function onGoodsSellTypeChange() {
    const type = document.getElementById('gd-sell-type').value;
    document.getElementById('gd-qty-label').textContent = type==='وزن'?'الوزن (كيلو)':'الكمية';
    document.getElementById('gd-sell-label').textContent = type==='وزن'?'سعر البيع (للكيلو)':(type==='مبلغ'?'المبلغ':'سعر البيع');
    document.getElementById('gd-cost-group').style.display = type==='مبلغ'?'none':'';
    document.getElementById('gd-qty-group').style.display = type==='مبلغ'?'none':'';
    document.getElementById('gd-total-paid-group').style.display = type==='وزن'?'':'none';
    if(type==='وزن') document.getElementById('gd-qty').step='0.001';
    else if(type==='قطعة') document.getElementById('gd-qty').step='1';
    updateGoodsPreview();
}
function updateGoodsPreview() {
    const type = document.getElementById('gd-sell-type').value;
    const qty = parseFloat(document.getElementById('gd-qty').value)||0;
    const cost = parseFloat(document.getElementById('gd-cost').value)||0;
    const sell = parseFloat(document.getElementById('gd-sell').value)||0;
    if(type==='وزن') {
        const total = qty * sell;
        document.getElementById('gd-total-paid').value = fmt(total);
    } else { document.getElementById('gd-total-paid').value = ''; }
    let total=0, profit=0, label='';
    if(type==='مبلغ') {
        total = sell; profit = 0; label = `الإجمالي: ${fmt(total)} ل.س (خدمة)`;
    } else {
        total = qty*sell; profit = qty*(sell-cost);
        label = `${qty} ${type==='وزن'?'كيلو':'قطعة'} × ${fmt(sell)} = ${fmt(total)} ل.س | ربح: ${fmt(profit)} ل.س`;
    }
    document.getElementById('gd-preview').textContent = label;
}
function addGoods(isDebt=false) {
    const category = document.getElementById('gd-category').value;
    const sellType = document.getElementById('gd-sell-type').value;
    const name = document.getElementById('gd-name').value.trim()||'—';
    const cost = parseFloat(document.getElementById('gd-cost').value)||0;
    const sell = parseFloat(document.getElementById('gd-sell').value)||0;
    const qty = sellType==='مبلغ'?1:(parseFloat(document.getElementById('gd-qty').value)||0);
    let totalSell, totalCost, profit;
    if(sellType==='مبلغ') {
        if(!sell){showToast('أدخل المبلغ','error');return;}
        totalSell=sell; totalCost=0; profit=0; // ربح صفر للخدمة
    } else if(sellType==='وزن') {
        if(!qty||!sell){showToast('أدخل الوزن والسعر','error');return;}
        totalSell=qty*sell; totalCost=qty*cost; profit=totalSell-totalCost;
    } else {
        if(!qty||!sell){showToast('أدخل الكمية والسعر','error');return;}
        totalSell=qty*sell; totalCost=qty*cost; profit=totalSell-totalCost;
    }
    goods.push({ id:Date.now(), category, sellType, unit:sellType==='وزن'?'كيلو':sellType==='مبلغ'?'خدمة':'قطعة', qty, cost, sell, totalCost, totalSell, profit, name, isDebt, time:nowTime(), fullDate:todayStr() });
    if(!isDebt) {
        if(sellType!=='مبلغ') {
            const st=goodsStock[category]||{qty:0}; st.qty=Math.max(0,(st.qty||0)-qty); goodsStock[category]=st;
        }
        const before=cashBalance; cashBalance+=totalSell;
        logCashTx('بضاعة', `بيع ${category} ${sellType==='وزن'?'('+qty+'ك)':''}`, +totalSell, before, cashBalance);
    }
    invHistory.push({ id:Date.now()+1, category, unit:sellType==='وزن'?'كيلو':'قطعة', qty:sellType==='مبلغ'?0:-qty, cost, sell, note:'بيع', fullDate:todayStr(), time:nowTime() });
    renderGoods(); saveLocal(); updateDashboard(); checkLowStock(); showToast('✅ تم تسجيل البيع');
}

// ═══════════════ QUICK SALES (المبيعات السريعة العامة) ═══════════════
function openQs2Modal() {
    document.getElementById('qs2-customer').value = '';
    document.getElementById('qs2-amount').value = '';
    document.getElementById('qs2-preview').textContent = 'المبلغ: 0 ل.س';
    document.getElementById('quickSaleModal').classList.add('active');
    document.getElementById('quickSaleModal').style.display='flex';
}
function calcQs2Total() {
    const amount = parseFloat(document.getElementById('qs2-amount').value)||0;
    document.getElementById('qs2-preview').textContent = `المبلغ: ${fmt(amount)} ل.س`;
}
function confirmQs2() {
    const customer = document.getElementById('qs2-customer').value.trim()||'—';
    const amount = parseFloat(document.getElementById('qs2-amount').value)||0;
    if(!amount){showToast('أدخل المبلغ','error');return;}
    quickSales.push({ id:Date.now(), customer, amount, fullDate:todayStr(), time:nowTime() });
    const before=cashBalance; cashBalance+=amount;
    logCashTx('بيع سريع (خدمة)', customer, +amount, before, cashBalance);
    saveLocal(); updateDashboard(); closeModal('quickSaleModal');
    document.getElementById('quickSaleModal').style.display='none';
    showToast(`✅ ${customer} — ${fmt(amount)} ل.س`);
}

// ═══════════════ CLIENT STATEMENT (مع quickSales) ═══════════════
function showClientStatement(clientId) {
    const client = clients.find(c=>c.id===clientId); if(!client) return;
    document.getElementById('cs-title').textContent = '📋 كشف حساب: '+client.name;
    const rows = []; const name = client.name.toLowerCase();
    // ... جمع البيانات من bills, credits, salaries, goods, debts
    // وإضافة quickSales
    quickSales.filter(q=>q.customer?.toLowerCase()===name).forEach(q=>{
        rows.push({ type:'بيع سريع', detail:q.customer, amount:q.amount, profit:0, date:q.fullDate, time:q.time });
    });
    _csAllRows = rows;
    // ... عرض الملخص والجدول
    document.getElementById('cs-filter').value='all';
    renderClientStatementTable(rows);
    document.getElementById('clientStatementModal').style.display='block';
}

// ═══════════════ باقي الدوال (دون تغيير) ═══════════════
// ... (renderAll, updateDashboard, إلخ)

// ═══════════════ INIT ═══════════════
loadLocal(); applyShopName(); updateHeaderDate();
setInterval(updateHeaderDate,60000); renderAll();
</script>
</body>
</html>
